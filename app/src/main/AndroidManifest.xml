<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carolina.xlambton">

    <!--
    android:icon="@mipmap/ic_launcher"
    android:icon="@drawable/homer_spy"

    android:roundIcon="@mipmap/ic_launcher_round"
    -->

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/homer_spy"
        android:label="@string/app_name"
        android:roundIcon="@drawable/homer_spy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewUser" />
        <activity android:name=".MenuOptionList" />
        <activity android:name=".AgentsList" />
        <activity android:name=".AgentProfile" />
        <activity android:name=".AgentHistorical" />
        <activity android:name=".MissionUpdate" />
        <activity android:name=".AgentsSearch" />
        <activity android:name=".AgentNewHistorical"></activity>
    </application>

</manifest>